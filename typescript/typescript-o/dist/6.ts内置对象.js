var regexp = /\w/;
var date = new Date();
var error = new Error();
var node = document.querySelectorAll("#aaa"); //节点数组
var htmls = document.body; //html元素
var divs = document.querySelector('div'); //指定标签
addEventListener('click', function (e) { });
function myPro() {
    return new Promise(function (res, rej) {
        res(1);
    });
}
